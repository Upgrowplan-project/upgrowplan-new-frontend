# Social Plan Master - Style Compatibility Guide

## 📋 Анализ совместимости стилей

### ✅ Проверено на конфликты

#### 1. **globals.css** (274 строки)

**Глобальные стили сайта, включают:**

- CSS переменные (`:root` colors)
- Body стили (font-family, margin, padding, flexbox)
- Кнопки контактов (.contact-btn)
- Кастомные классы (.text-brand, .nav-link-custom)
- Стили footer, main
- **Важно**: Все input/select/textarea/button используют `font-family: inherit`

**Поиск конфликтов**: ❌ **Нет конфликтов**

- globals.css использует глобальные селекторы и классы
- Social Plan Master использует CSS Modules (изолированные стили)

#### 2. **Bootstrap 5.3.7** (подключено в layout.tsx)

```tsx
import "bootstrap/dist/css/bootstrap.min.css";
```

**Bootstrap компоненты/стили, которые влияют:**

- Grid система (.container, .row, .col)
- Button стили (.btn, .btn-primary)
- Form элементы (.form-control, .form-label)
- Spacing utilities (m-_, p-_, gap-\*)
- Responsive breakpoints (xs, sm, md, lg, xl)

**Поиск конфликтов**: ✅ **БЕЗОПАСНО**

- Social Plan Master не использует Bootstrap классы
- Использует CSS Modules → стили не глобальные
- Может спокойно добавить Bootstrap компоненты если нужно

#### 3. **react-bootstrap** (^2.10.10)

**TypeScript компоненты React поверх Bootstrap**

**Поиск конфликтов**: ✅ **БЕЗОПАСНО**

- Social Plan Master не импортирует react-bootstrap компоненты
- Использует собственные HTML элементы и CSS Modules
- Может использовать react-bootstrap если понадобится

---

## 🎨 Архитектура стилей Social Plan Master

### CSS Modules

```
socialPlanMaster.module.css (CSS Modules)
├── Все классы scoped (изолированы)
├── Не влияют на другие страницы
├── Не подвержены влиянию globals.css
└── Safe to customize
```

### Иерархия подключения CSS

```
1. bootstrap/dist/css/bootstrap.min.css    (в layout.tsx)
2. aos/dist/aos.css                         (в layout.tsx)
3. bootstrap-icons/font/bootstrap-icons.css (в layout.tsx)
4. app/globals.css                          (в layout.tsx)
   └─ Глобальные стили сайта
   └─ Определяет box-sizing: border-box
   └─ Наследуемые свойства font-family
5. socialPlanMaster.module.css              (импортируется в page.ru.tsx)
   └─ CSS Modules - локальные стили
   └─ Полностью независимы от 1-4
```

---

## 🔧 Проверочный список (Validation)

### CSS Box Model

- ✅ globals.css: `* { box-sizing: border-box; }` (строка 118)
- ✅ socialPlanMaster.css: `.input/textarea { box-sizing: border-box; }` (явное)
- ✅ **Конфликт?** НЕТ (наследование работает правильно)

### Font Family

- ✅ globals.css: `body { font-family: Arial, Helvetica, sans-serif; }`
- ✅ globals.css: `input/select/textarea/button { font-family: inherit; }`
- ✅ socialPlanMaster.css: использует `font-family: inherit` (закомментировано в коде как напоминание)
- ✅ **Конфликт?** НЕТ

### Цветовая схема

- ✅ globals.css использует: `--background`, `--foreground` (CSS переменные)
- ✅ Social Plan Master использует: `#1e6078`, `#0785f6` (inline hex colors)
- ✅ Market Research использует: те же цвета (согласованность!)
- ✅ **Конфликт?** НЕТ

### Input/Textarea стили

- ✅ globals.css: базовые `font-family: inherit`
- ✅ Social Plan Master: добавляет padding, border, border-radius, transition
- ✅ **Конфликт?** НЕТ (только дополнение)

### Button стили

- ✅ globals.css: только .contact-btn (специфичный класс)
- ✅ Social Plan Master: .submitButton, .downloadBtn, .resetBtn (собственные)
- ✅ **Конфликт?** НЕТ

### Responsive Breakpoints

- ✅ globals.css: `@media (max-width: 767px)` для navbar
- ✅ Social Plan Master: `@media (max-width: 768px)` для контента
- ✅ Bootstrap: xs, sm (≤576px), md (≥768px), lg (≥992px), xl (≥1200px)
- ✅ **Конфликт?** НЕТ (совместимо)

---

## 📦 Важные замечания

### 1. CSS Modules - Полная изоляция

```tsx
// page.ru.tsx
import styles from "./socialPlanMaster.module.css";

// Используется как:
<div className={styles.container}>...</div>

// Результат: класс будет переименован в:
<div className="_socialPlanMaster_container__abc123">...</div>
```

**Результат**:

- ✅ Стили изолированы - не влияют на другие компоненты
- ✅ Нет конфликтов с globals.css
- ✅ Нет конфликтов с Bootstrap
- ✅ Safe for all environments

### 2. Market Research Page - Полная совместимость

Social Plan Master точно скопирован с Market Research:

- ✅ Использует одинаковые паттерны CSS
- ✅ Использует одинаковые цвета (#1e6078, #0785f6)
- ✅ Использует одинаковые структуры (Form → Progress → Results)
- ✅ Использует одинаковые breakpoints
- ✅ Обе страницы на одном уровне - `/solutions/`

### 3. Наследование и Каскадирование

```
globals.css (body styles)
    ↓ наследуется в Layout
    ↓ наследуется в Page Component
    ↓ CSS Modules переопределяют локально (без конфликтов)
```

---

## 🚀 Рекомендации

### DO ✅

- ✅ Используйте CSS Modules для новых стилей
- ✅ Наследуйте font-family через `inherit`
- ✅ Используйте цвета из globals.css если нужна унификация
- ✅ Используйте Bootstrap breakpoints (768px, 992px)
- ✅ Добавляйте react-bootstrap компоненты если нужны

### DON'T ❌

- ❌ Не переопределяйте глобальные селекторы (body, html)
- ❌ Не добавляйте `!important` без необходимости
- ❌ Не используйте глобальные классы (используйте CSS Modules)
- ❌ Не устанавливайте font-family напрямую (наследуйте)
- ❌ Не забывайте про mobile-first подход

---

## 🧪 Тестирование

### Проверка на конфликты

```bash
# 1. Открыть DevTools (F12)
# 2. Инспектировать элемент (.container, .input, .button)
# 3. Проверить, что стили применены корректно:
#    - Нет красных линий в CSS
#    - Нет конфликтующих правил
#    - Все стили из socialPlanMaster.module.css

# 4. Проверить responsive:
#    - Резайзить window до 768px
#    - Проверить, что стили меняются (mobilе layout)

# 5. Проверить form inputs:
#    - Клацнуть на input → focus border должен быть #1e6078
#    - Клацнуть на textarea → focus border должен быть #1e6078
```

---

## 📚 Файлы и строки

| Файл                        | Строки | Описание                      |
| --------------------------- | ------ | ----------------------------- |
| globals.css                 | 1-276  | Глобальные стили              |
| [locale]/layout.tsx         | 1-5    | Bootstrap/globals подключение |
| socialPlanMaster.module.css | 1-620  | Локальные стили (CSS Modules) |
| page.ru.tsx                 | 1-3    | Импорт CSS Modules            |

---

## ✨ Итог

**Status: ✅ FULLY COMPATIBLE**

- ✅ Нет конфликтов с Bootstrap 5.3.7
- ✅ Нет конфликтов с globals.css
- ✅ Нет конфликтов с react-bootstrap
- ✅ Используется CSS Modules для изоляции
- ✅ Совместимо с Market Research стилями
- ✅ Responsive design работает корректно
- ✅ Inherited styles (font-family) работают
- ✅ Custom colors не конфликтуют

🎉 **Страница готова к использованию!**
